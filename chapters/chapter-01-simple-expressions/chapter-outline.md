# Chapter 1: Simple Expressions

This chapter introduces simple numeric expressions. The tests establish that Trivial supports integer and floating-point literals, negative numbers, and the four basic arithmetic operations.

The suite also introduces operator precedence and parentheses. At that point, a small language needs a real parser instead of simple token-by-token evaluation. By the end of this chapter, students should understand that expressions evaluate to values and that grouping changes meaning.
